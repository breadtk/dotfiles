# Place this file at ~/.gitconfig
[user]
    name = osman
    email = osman@surkatty.org
[core]
    editor = nvim
[alias]
    co = checkout
    ci = commit -a
    st = status
    br = branch
    hist = log --pretty=format:\"%h %ad | %s%d [%an]\" --graph --date=short
    type = cat-file -t
    dump = cat-file -p
[push]
    default = simple

# Github uses "Personal Access Tokens" as passwords for their HTTPS protocol.
# You can generate it at: https://github.com/settings/tokens
[url "https://breadtk:ghp_EXAMPLEEXAMPLEEXAMPLE@github.com"]
    insteadOf = https://github.com
